"""Package-private helpers for the ingestion L1 layer.

Anything under `_internal/` is forbidden to be imported from other packages
(CODING_STANDARDS §2.3). Only sibling modules in `packages/ingestion/`
may use these helpers.
"""

"""Package-private helpers for the orchestration L5 layer.

Anything under `_internal/` is forbidden to be imported from other packages
(CODING_STANDARDS §2.3). Only sibling modules in `packages/orchestration/`
may use these helpers.
"""
